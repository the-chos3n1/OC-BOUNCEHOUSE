# Public site URL — no trailing slash. Used for canonical URLs, OG, sitemap, and JSON-LD.
# NEXT_PUBLIC_SITE_URL=https://www.your-domain.com

# Optional: when set, booking page shows "keys detected" messaging.
# Implement Checkout Session creation server-side before taking live payments.
# STRIPE_SECRET_KEY=sk_test_...
